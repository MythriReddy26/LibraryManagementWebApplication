@model IEnumerable<LibraryManagementWebApplication.Models.Profile>

@{
    ViewData["Title"] = "Profiles";
}

<h1>Profiles</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OldPassword)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewPassword)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfrimPassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OldPassword)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewPassword)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfrimPassword)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
